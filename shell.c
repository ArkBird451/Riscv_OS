#include "user.h"

void main(void) {
    // Demonstration of tar file system using inline system calls
    // Note: Function calls from main() have issues, so we use inline assembly
    
    for (;;);  // Keep process alive
}